"""
Screen coordinates for MinecraftStronghold automation.

How to fill these in:
1. Run pointer.py.
2. Hover your mouse over each button/location described below.
3. Read the X/Y values pointer.py prints in the terminal.
4. Put those values into the matching dict below.
"""

create_world = {
    "x": 0,
    "y": 0,
}  # "Create New World" button on the main menu

allow_commands = {
    "x": 0,
    "y": 0,
}  # "Allow Commands" toggle in the world creation menu

create_new_world_btn = {
    "x": 0,
    "y": 0,
}  # "Create New World" confirm button

seed_coord = {
    "x": 0,
    "y": 0,
}  # Location to click on the seed message in chat

str_coord = {
    "x": 0,
    "y": 0,
}  # Location to click on the stronghold coordinates message in chat
